# Protocol fixtures

These small JSONL files are versioned examples of the app-server messages consumed by the prototype. `process-exit.jsonl` and `timeout.jsonl` describe transport scenarios rather than server messages; the fake app-server integration tests exercise the actual process exit and no-response behavior.
